[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    unit: pure unit tests, no infrastructure needed
    integration: requires test database
    system: full application stack, module-scoped DB
    regression: regression suite — runs in CI on every commit

addopts = --strict-markers -v
